[build]
builder = "NIXPACKS"
buildCommand = "cd apps/shelfsense-mock-api && pip install -r requirements.txt"

[deploy]
startCommand = "cd apps/shelfsense-mock-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
